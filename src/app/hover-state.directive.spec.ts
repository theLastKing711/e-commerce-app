import { HoverStateDirective } from './hover-state.directive';

describe('HoverStateDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverStateDirective();
    expect(directive).toBeTruthy();
  });
});
